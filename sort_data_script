echo "Sorting Data:"

cd unlabelled/

for FILE in $(ls);
do
	echo $FILE
	name=$(echo "$FILE"| cut -f 1 -d '.')
	
	LABEL="${name}.txt"
	echo "looking for $LABEL ..."
	if [ -f $LABEL ]; then
		echo "found"
		mv $FILE ../labelled/
		mv $LABEL ../labelled/
	else
		echo "not found"
	fi
done;
